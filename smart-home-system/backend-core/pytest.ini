[tool.pytest.ini_options]
; backend-core pytest 配置
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short --no-header
timeout = 60
asyncio_mode = auto
